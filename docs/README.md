# docs

- web_client.md: is about all possibles applications which can act as web_client, i.e. which can send http requests to a web server and receive http responses

- localhost.md: is about the special address 127.0.0.1, aka localhst,
